Subiectul 1:
1. d) (x<=8) and (x>5)
2. a) 963
   b) 61,65,67
   c)for i in range(0, len(str(n))):
    c = n % 10
    n = n // 10
    if c % 3 == 0:
        z += p * (9 - c)
        p = p * 10
    print(z)
  d) n = int(input())
      z = 0
      p = 1
      while n > 0:
          c = n % 10
          n = n // 10
          if c % 3 == 0:
              z += p * (9 - c)
              p = p * 10
      print(z)
